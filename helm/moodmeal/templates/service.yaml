apiVersion: v1
kind: Service
metadata:
  name: { { include "moodmeal.fullname" . } }
  labels: { { - include "moodmeal.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "moodmeal.selectorLabels" . | nindent 4 } }
